
// This file was generated by Prisma and assumes you have installed the following:
// npm install --save-dev prisma dotenv
import * as dotenv from "dotenv";
dotenv.config({ path: "src/modules/catalog/.env" });
import { defineConfig, env } from "prisma/config";

// ============================================
// CATALOG DOMAIN PRISMA CONFIG
// ============================================
// When switching to another domain, update these paths:
// - Auth: src/modules/auth/prisma/...
// - Inventory: src/modules/inventory/prisma/...
// - Orders: src/modules/orders/prisma/...

export default defineConfig({
  schema: "src/modules/catalog/prisma/schema.prisma",

  migrations: {
    path: "src/modules/catalog/prisma/migrations",
  },
  engine: "classic",
  datasource: {
    url: env("DATABASE_URL"),
  },
});
